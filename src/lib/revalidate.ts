/**
 * ISR revalidation intervals (seconds).
 * Next.js requires literal `export const revalidate = N` in route files.
 */
export const REVALIDATE_BLOG = 60 * 60 * 24; // 24 hours
export const REVALIDATE_MARKETING = 60 * 60; // 1 hour
export const REVALIDATE_PRODUCTS = 60 * 30; // 30 minutes
