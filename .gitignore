# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.next/

# Originals of bulk-resized images (kept on disk for restore; not committed)
image-backups/

# Supabase CLI temporary files
supabase/.temp/
merchant-feed.xml

# Scratch copy of the repo that tooling drops here (~38MB, carries its own .git and .env).
# Untracked and unignored it was one `git add .` away from committing a second env file.
.mainbase/
